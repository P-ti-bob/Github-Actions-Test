name: Démonstration Pipeline CI

on:
  push:
    branches:
      - main
jobs:
  demo:
    name: Jobs Demo
    runs-on: ubuntu-latest
    steps:
      - name: Première étape
        run: echo Hello!

      - name: Script plus complexe
        run: |
          echo "Début du script"
          echo "Étape 1: Clone le dépôt"
test:
  needs: demo
  runs-on: ubuntu-latest
  steps:
    - name: Récupération code source
      uses: actions/checkout@v4
